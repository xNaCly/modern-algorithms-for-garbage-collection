Java examples
-----------

All examples written in Java in this paper are included in this directory. To
find a section of the paper the code example references to, take a look at
the comment at the first line. 

Run a given example via javac using make:

    make Scope

Every target is named after the filename, excluding its extension.
